 name: Hello
 on: push
 jobs:
  print:
    runs-on: ubuntu-latest
    env:
      USERNAME: ${{ vars.USERNAME }}
      PASSWORD: ${{ vars.password }}
    steps:
      - run: echo "${USERNAME}"
      - run: echo "${PASSWORD}"